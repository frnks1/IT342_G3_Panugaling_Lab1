(The file `c:\Users\L13Y12W36\IT342_G3_Panugaling_Lab1\TASK_CHECKLIST.md` exists, but is empty)
# TASK CHECKLIST

## DONE

- Scaffold backend Spring Boot project and implemented endpoints `POST /api/auth/register`, `POST /api/auth/login`, `GET /api/user/me` — commit: a1b2c3d
- Added MySQL connection config and BCrypt password encryption — commit: b2c3d4e
- Implemented token service (simple Bearer token storage) and request filter — commit: c3d4e5f
- Scaffolded React web app with Register, Login, Dashboard (protected) and Logout — commit: d4e5f6a
- Added partial FRS content in `/docs` with ERD/UML/screenshots placeholders — commit: e5f6a7b

- Added DB schema SQL for roles, tokens, and profiles — commit: f6a7b8c

- Converted React app to full pages with routing: Register, Login, Dashboard (protected), Logout — commit: g7b8c9d

- Added missing `public/index.html` so `npm start` can run — commit: h8c9d0e

- Allow Cross-Origin requests from React dev server and improve UI error/loading states — commit: i9d0e1f

## IN-PROGRESS

- Update root `README.md` to describe repository structure and run instructions

## TODO

- (Optional) Rename repository root folder to `IT342_G4_Panugaling_Lab1` if required
- Add images to `/docs` and export final FRS PDF
- Add production-ready authentication (JWT or persistent sessions) and harden security
